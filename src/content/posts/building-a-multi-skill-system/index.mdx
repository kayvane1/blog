---
title: Skill orchestrations, and what I like about pstack
date: 2026-05-28
summary: A teardown of pstack by Lauren Tan, and the extensibility move I think will turn skill orchestrations into continual learning loops.
tags: [agents, agentic-coding, tooling, claude, productivity]
github: https://github.com/cursor/plugins/tree/main/pstack
---

import { Tldr } from "../../../components/Tldr";
import { SkillGraph } from "./SkillGraph";

<Tldr>
  Engineers are publishing their internal workflows as composable skill systems — first
  superpowers, then Matt Pocock's library, and this week pstack from Lauren Tan
  ([@poteto](https://x.com/poteto)), shipped under Cursor's plugins repo. Pstack is organised as
  three layers: a workflow that picks a playbook, playbooks that lay out the steps an agent has to
  walk through, and principles that ground how the agent behaves at each step. This structure allows for more composability of reusable principles. 
  With the right harness watching your traces and PRs, this is the right shape for a continual learning loop.
</Tldr>

---

## The skill-orchestration wave

A few engineers I follow have started publishing their day-to-day workflows as skill systems you can install and run. The first one that really landed for me was [superpowers](https://github.com/obra/superpowers). It really helped me to understand skill composition and see the power of them being harnessed correctly (this was with Sonnet 4!)

Then Matt Pocock's [skills library](https://github.com/mattpocock/skills) showed up and added another layer to my mental model. What I like about Matt's skills is that they're drawn from engineering first principles, with a lot of the ideas pulled from engineering books and distilled into small, composable, targeted skills you can actually reach for when the scenario calls for it. The two I use most are `grill-me` and `improve-codebase-architecture` — the second one I lean on a lot when I'm trying to reason about abstractions in a codebase.

This week Lauren Tan ([@poteto](https://x.com/poteto)) published [pstack](https://github.com/cursor/plugins/tree/main/pstack) under Cursor's plugins repo. It's another one of these skill-orchestration plugins, and I wanted to get familiar with it because the description promised to solve a few of the pain points I've been running into with this generation of agentic coding tools.

## How it's structured

Pstack is a three-layer system.

- A **workflow** at the top — one skill (`/poteto-mode`) that reads your request and routes.
- A set of **playbooks** in the middle — recipes for the shape of work in front of you (bug fix, feature, refactor, perf, prototype, and so on). Each playbook is a numbered sequence of steps the agent walks through.
- A set of **principles** at the bottom — single-rule leaf skills that ground how the agent should behave at each step.

The interactive diagram below maps the whole thing. Picking a playbook unfolds its execution DAG — the numbered steps that get copied into the agent's todolist verbatim, along with the workflow skills called at each step and the principles each step applies. Picking a workflow skill or a principle keeps the catalog view and lights up everything that uses it.

<SkillGraph />

The orchestrator is a very simple markdown-based mapping table — if the task looks like `X`, use that playbook.

Playbook steps get copied **verbatim** into the agent's working todolist before it reasons about the task. The skill is explicit that paraphrasing is the failure mode it's structurally avoiding. You can skip a step, but only with a stated reason — silent drops aren't allowed. This reminds me of how superpowers also leverages checklists as part of the initiation phase to ensure the longer term context of what needs to happen is sticky both for the agent but also gives forsight to the end user as checklists have seperate UI components that render immediately when triggered.

Principles sit one tier below the playbooks, each in its own file. They're indexed inline by the orchestrator and cited by name at the points where they apply. Every principle skill (and the orchestrator itself, and most of the workflow skills) is shipped with `disable-model-invocation: true`. That flag tells the harness not to auto-trigger the skill based on a keyword match in the user's message, principles are applied intentionally to each playbook, this is also key for the composability principle, where new principles can be added and explicitly added to relevant playbooks, principles won't impact any other skill you have installed.

## Poteto -> n Poteto

When you trigger the plugin through `/poteto-mode` it has the ability to istelf to fan work out to a child (a code-writing delegate, a parser for a large artifact, an ad-hoc helper) it does it through a `Task` call with `subagent_type: "poteto-agent"`. And the `poteto-agent` definition itself is six lines. Its entire body is "read `poteto-mode/SKILL.md` in full before any work, including its inline Principles index".
The pattern is: parent's on a thinking model, reviewing the work. Three or four code-writing children get spawned on a faster, cheaper model. All of them entered through the same six-line wrapper, re-read the same SKILL.md, navigated to the same principle leaves before touching the diff. The parent didn't have to repeat the house style in the child prompts because the wrapper handles that.
A couple of edges that are worth knowing. Some workflow skills (`how`, `why`, `interrogate`, `reflect`) configure their _own_ `subagent_type` because their value comes from mixing model families adversarially. The rule is to default to `poteto-agent` for direct delegates inside a playbook step, but respect the routed skill's own prescription when you're going through one of them.

## Principles are extendible

As you watch your agent fail in new ways, you add principles to catch those failure modes and link them from the playbook steps where the failure showed up. Today that's a manual edit, but it doesn't have to be.

If you had your own version of pstack (see `automate-me`) — same shape, customised to your tools and your team's conventions — you could close the loop. A background automation (a Codex run, a scheduled agent, whatever you have) reads your recent traces and your merged PRs, looks for failure modes that keep recurring, drafts a candidate principle, and links it to the playbook step where it would have fired. You sign off on it or you don't. The plugin learns from the team it's working alongside.

I find that really powerful as an abstraction. The slot's already there for it — you don't have to rewrite the orchestrator or any playbook prose, you just add the principle and reference it from the step. And because modern agent harnesses can run analysis across many threads at once, the loop is genuinely tractable rather than a thought experiment.

The library already has a principle for this behaviour `principle-encode-lessons-in-structure`

## The diffs are smaller

I've felt that the latest generation of models produce a higher volume of code to fix a bug or ship a feature. This may be a harness problem, where some of my context for previous generations is making the models paranoid and so they are writing more defensive code again with more comments to explain what they're doing but that has had a chain reaction in PR complexity — it's becoming the norm to see much larger diffs land, which makes them harder to review and quietly increases the cognitive debt sitting on the team.
When I read the `pstack` release blog post, I like that there was emphasis to bias the model toward minimal diffs that solves the problem and keep the codebase maintainable. 
Once I dug in, there were a few structural patterns that I thought were very clever and can see why there has been a high level of adoption within the Cursor team.

## How I'm wiring this up in Codex:

I have a daily scheduled task in Codex called "Self-Improving pstack". It runs at 9:00 against the previous 24 hours of sessions, in a worktree, with this prompt:

> Review all threads in the last 24 hours that used `pstack` and identify implementation issues that were either identified by me or in the code review and suggest if any new `skill`, `lint-rules` or `principles` can be added to avoid the same failure mode reoccuring in future sessions. Ground yourself in the `principle-encode-lessons-in-structure` principle.

I'll write an update on what it found and how my setup has evolved over time.

I also want to try [`automate-me`](https://github.com/cursor/plugins/blob/main/pstack/skills/automate-me/SKILL.md) which is a similar kind of idea to create a more personalised workflow for `<you>-mode` 


